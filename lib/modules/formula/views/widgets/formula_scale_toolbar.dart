import 'package:flutter/material.dart';

/// 配方管理顶部工具栏组件
class FormulaScaleToolbar extends StatelessWidget {
  final Widget child;

  const FormulaScaleToolbar({
    super.key,
    required this.child,
  });

  @override
  Widget build(BuildContext context) {
    return child;
  }
}
