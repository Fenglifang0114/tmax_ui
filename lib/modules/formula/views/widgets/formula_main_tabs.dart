import 'package:flutter/material.dart';

/// 配方/原料/暂存 核心选项卡内容区 Widget
class FormulaMainTabs extends StatelessWidget {
  final Widget child;

  const FormulaMainTabs({
    super.key,
    required this.child,
  });

  @override
  Widget build(BuildContext context) {
    return child;
  }
}
