import 'package:flutter/material.dart';

/// 电子秤串口状态侧边栏 Widget
class ScaleStatusSidebar extends StatelessWidget {
  final Widget child;

  const ScaleStatusSidebar({
    super.key,
    required this.child,
  });

  @override
  Widget build(BuildContext context) {
    return child;
  }
}
